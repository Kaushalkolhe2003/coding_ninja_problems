#include <algorithm>
int missingNumber(vector<int>&a, int N) {
   
   int sum=(N*(N+1))/2;
   int c=0;
   for(int i=0;i<a.size();i++){
       c+=a[i];
   }
   int missingNum = sum -c;
    return missingNum;
}
